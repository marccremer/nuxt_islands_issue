{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare && patch-package"
  },
  "dependencies": {
    "nuxt": "3.10.0",
    "patch-package": "^8.0.0",
    "vue": "^3.4.15",
    "vue-router": "^4.2.5"
  }
}
